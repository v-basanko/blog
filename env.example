# --- Core ---
NODE_ENV=development
BASE_URL=http://localhost:3000

# --- Database (Prisma/PostgreSQL) ---
# For the included docker-compose: user=postgres, password=admin, db=postgres
DATABASE_URL=postgresql://postgres:admin@localhost:5432/postgres?schema=public

# --- Auth.js / NextAuth v5 ---
# Required in production:
AUTH_SECRET=changeme-long-random-string
# Canonical URL for auth callbacks (prod):
AUTH_URL=http://localhost:3000
# If behind a proxy/CDN (Vercel, Nginx), recommend:
AUTH_TRUST_HOST=true

# --- OAuth Providers (optional but recommended) ---
# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# --- Emails (Resend) ---
RESEND_API_KEY=
