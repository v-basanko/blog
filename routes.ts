export const publicRoutes = ['/', '/email-verification', '/forgot-password', '/reset-password'];

export const authRoutes = ['/login', '/register'];

export const apiAuthPrefix = '/api/auth';

export const LOGIN_REDIRECT = '/user/1';